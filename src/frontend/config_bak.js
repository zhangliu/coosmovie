export default {
  apiUrl: 'http://127.0.0.1:8001/src/backend/web/index.php',
  // localUrl: 'http://127.0.0.1:8001/src/backend/',
  qiniuUrl: 'http://oertbv1rg.bkt.clouddn.com/',
  iflyInfo: {
    maxVolume: 30,
    params: {
      'grammar_list': null,
      'params': 'appid=577ca2ac,appidkey=9a77addd1154848d, lang = sms, acous = anhui, aue=speex-wb;-1, usr = mkchen, ssm = 1, sub = iat, net_type = wifi, rse = utf8, ent = sms-en16k, rst = plain, auf  = audio/L16;rate=16000, vad_enable = 1, vad_timeout = 5000, vad_speech_tail = 500, compress = igzip',
      //"params": "appid=584a9c8a,appidkey=5cb7ef11af53e6bc, lang = sms, acous = anhui, aue=speex-wb;-1, usr = mkchen, ssm = 1, sub = iat, net_type = wifi, rse = utf8, ent =sms-en, rst = plain, auf  = audio/L16;rate=16000, vad_enable = 1, vad_timeout = 5000, vad_speech_tail = 500, compress = igzip",
    },
  },
}
